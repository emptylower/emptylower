# GitHub Profile Metrics — Sakura Night Theme
# Each metric is a separate job so you can re-run individual failures
# plugins_errors_fatal: yes ensures broken SVGs never overwrite good ones

name: GitHub Metrics

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:

  base:
    runs-on: ubuntu-latest
    environment: production
    permissions: { contents: write }
    steps:
      - uses: lowlighter/metrics@latest
        with:
          filename: github-metrics/base.svg
          token: ${{ secrets.METRICS_TOKEN }}
          user: emptylower
          template: classic
          config_timezone: Asia/Shanghai
          base: "header, activity, community, repositories"
          plugin_traffic: yes
          plugin_lines: yes
          plugin_introduction: yes
          plugin_gists: yes
          plugins_errors_fatal: yes

  isocalendar:
    runs-on: ubuntu-latest
    environment: production
    permissions: { contents: write }
    steps:
      - uses: lowlighter/metrics@latest
        with:
          filename: github-metrics/isocalendar.fullyear.svg
          token: ${{ secrets.METRICS_TOKEN }}
          base: ""
          plugin_isocalendar: yes
          plugin_isocalendar_duration: full-year
          plugins_errors_fatal: yes

  languages:
    runs-on: ubuntu-latest
    environment: production
    permissions: { contents: write }
    steps:
      - uses: lowlighter/metrics@latest
        with:
          filename: github-metrics/languages.indepth.svg
          token: ${{ secrets.METRICS_TOKEN }}
          base: ""
          plugin_languages: yes
          plugin_languages_ignored: >-
            html, css, tex, less, dockerfile, makefile, qmake, lex, cmake, shell,
            gnuplot
          plugin_languages_indepth: yes
          plugin_languages_details: lines, bytes-size
          plugin_languages_limit: 6
          plugin_languages_analysis_timeout: 15
          plugins_errors_fatal: yes

  stargazers:
    runs-on: ubuntu-latest
    environment: production
    permissions: { contents: write }
    steps:
      - uses: lowlighter/metrics@latest
        with:
          filename: github-metrics/stargazers.chartist.svg
          token: ${{ secrets.METRICS_TOKEN }}
          base: ""
          plugin_stargazers: yes
          plugin_stargazers_charts_type: chartist
          plugins_errors_fatal: yes

  stars:
    runs-on: ubuntu-latest
    environment: production
    permissions: { contents: write }
    steps:
      - uses: lowlighter/metrics@latest
        with:
          filename: github-metrics/stars.svg
          token: ${{ secrets.METRICS_TOKEN }}
          base: ""
          plugin_stars: yes
          plugin_stars_limit: 10
          plugins_errors_fatal: yes

  followup:
    runs-on: ubuntu-latest
    environment: production
    permissions: { contents: write }
    steps:
      - uses: lowlighter/metrics@latest
        with:
          filename: github-metrics/followup.indepth.svg
          token: ${{ secrets.METRICS_TOKEN }}
          base: ""
          plugin_followup: yes
          plugin_followup_indepth: yes
          plugins_errors_fatal: yes

  reactions:
    runs-on: ubuntu-latest
    environment: production
    permissions: { contents: write }
    steps:
      - uses: lowlighter/metrics@latest
        with:
          filename: github-metrics/reactions.svg
          token: ${{ secrets.METRICS_TOKEN }}
          base: ""
          plugin_reactions: yes
          plugin_reactions_limit: 100
          plugin_reactions_details: percentage
          plugins_errors_fatal: yes

  calendar:
    runs-on: ubuntu-latest
    environment: production
    permissions: { contents: write }
    steps:
      - uses: lowlighter/metrics@latest
        with:
          filename: github-metrics/calendar.full.svg
          token: ${{ secrets.METRICS_TOKEN }}
          base: ""
          plugin_calendar: yes
          plugin_calendar_limit: 2
          plugins_errors_fatal: yes

